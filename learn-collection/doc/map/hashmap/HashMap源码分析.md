#### 


#### 构造方法

#### put
    1.hash(key)对key的hashcode做高16位的异或运算
    2.判断散列表是否为空，为空调用resize()方法初始化
    3.判断是否发生hash碰撞
        1.没有碰撞直接插入
        2.发生碰撞
            1.如果key的hash值与key值都相同的话，替换旧值
        2.
        3.判断key对应value值得类型是否为红黑树
            1.
        
#### 其他说明
    transient 关键字标记的成员变量不参与序列化过程